# Kraft

A Java 21 Gradle application.

## Requirements

- JDK 21 (or let Gradle auto-provision it via the Java toolchain)

## Run

```bash
./gradlew run
```

On Windows:

```bat
gradlew.bat run
```

## Build

```bash
./gradlew build
```
